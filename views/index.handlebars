<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center" id="top-bar">
      <img src="assets/images/burger.png">
      <h1>Eat A Burger</h1>
    </div>
    <div class="col-xs-6" class="task">
      <div class="row">
        {{#each burger_data}} {{#unless this.devoured}}
        <div class="col-xs-6" id="uneaten">
          <pre>{{this.id}}.{{this.burger_name}}</pre>
        </div>
        <div class="col-xs-3 text_center">
          <form method="POST" action="/update?_method=PUT" class="button-size">
            <input type="hidden" name="burger_id" type="text" value={{this.id}}>
            <br>
            <button type="submit" class="btn btn-default">
              Ate it!</button>
          </form>
        </div>
        {{/unless}} {{/each}}
      </div>
    </div>

    <div class="col-xs-6 text-center" class="task" id="eaten">
      {{#each burger_data}} {{#if this.devoured}}
      <input class="form-control" type="text" placeholder="{{this.id}}. {{this.burger_name}}"> {{/if}} {{/each}}

    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 text-center enter_selection">
      <p>Add a name of a burger you're wanting to eat</p>
      <form action="/create" method="POST">
        <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
        <button type="submit" class="btn btn-primary" id="text-enter-button">SUBMIT</button>
      </form>

    </div>
  </div>
</div>